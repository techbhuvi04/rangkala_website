# Dependencies
node_modules/

# Environment / secrets — NEVER commit!
.env
.env.local
.env.*.local
*firebase-adminsdk*.json

# Local uploads (not used on Vercel — Cloudinary handles this in production)
public/uploads/*
!public/uploads/.gitkeep

# OS / editor
.DS_Store
*.log

# Deprecated Python backend + old frontend — not part of the deployed app
old_page/
